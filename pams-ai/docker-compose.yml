
services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: pams
      POSTGRES_PASSWORD: pams
      POSTGRES_DB: pams_ai
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
